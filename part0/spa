```mermaid
sequenceDiagram
    participant Browser
    participant Server

    Browser->>Server: GET /spa
    Server-->>Browser: HTML page (spa)
    
    Browser->>Server: GET /main.css
    Server-->>Browser: CSS file

    Browser->>Server: GET /main.js
    Server-->>Browser: JS file

    Note over Browser: JS executes and sets up the SPA

    Browser->>Server: GET /data.json
    Server-->>Browser: JSON with notes

    Note over Browser: JS renders the notes dynamically

```